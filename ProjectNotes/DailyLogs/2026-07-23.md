# 2026-07-23 — Daily Log

## Session Overview

Separation-of-concerns review starting from the data model and working up through repository interfaces, managers, DTOs, controllers, and tests. No missing implementations were found — all 45 entity repositories, interfaces, and manager wiring were already complete. Work focused on correcting structural issues, improving the codebase, and significantly expanding test coverage.

Branch: `feature/organization-enhancements`

---

## Items Completed

### 1. Option B — Remove Write Methods from `IRepositoryBase` / `RepositoryBase`

**Problem:** `Create`, `Update`, and `Delete` in `RepositoryBase<T>` created a new `DbContext`, staged changes, then immediately disposed without calling `SaveChanges`. Nothing was ever persisted. Additionally, `IRepositoryManager.SaveChangesAsync` threw `NotImplementedException`. Both were silent, broken dead code.

**Resolution (Option B — explicit read-only repositories):**
- Removed `Create`, `Update`, `Delete` from `IRepositoryBase<T>` and `RepositoryBase<T>`
- Removed the private `GetById(BenDataContext, Guid)` helper that was only used by `Delete`
- Removed `SaveChangesAsync` from `IRepositoryManager` and `RepositoryManager`
- Repositories are now explicitly read-only; all write operations live in controllers via `IDbContextFactory<BenDataContext>` — which was already 100% of actual usage
- Removed the `SaveChangesAsync_Throws_NotImplemented` test that was asserting the broken behaviour

---

### 2. Geocoding — Move from Dead Repository Overrides to Admin Controllers

**Problem:** `OrganizationAddressRepository` and `UserAddressRepository` overrode `Create` and `Update` to apply geocoding, then called `base.Create/Update`. Since base never saved, geocoding never executed.

**`AddressGeocodingService` changes:**
- Changed from `internal static` → `public static` so WebApi can reference it
- `GeocodingLookupResult` changed from `internal sealed record` → `public sealed record`
- Replaced blocking `TryResolveCoordinates` (used `GetAwaiter().GetResult()`) with fully async `TryResolveCoordinatesAsync(string, string?, string, string, string, string, CancellationToken)`
  — uses `await HttpClient.GetAsync(url, cancellationToken)` and `await response.Content.ReadAsStringAsync(cancellationToken)` throughout

**`AdminOrganizationAddressController` and `AdminUserAddressController`:**
- Override `Create` and `Update` from `AdminEntityControllerBase`
- Each override calls `await ApplyGeocodingAsync(entity, cancellationToken)` before delegating to `base`
- Private static `ApplyGeocodingAsync` populates `Latitude`, `Longitude`, `GeocodingResponseJson`, `GeocodingResultType` on the entity

**Dead overrides in repositories:**
- Removed `Create`/`Update` from `OrganizationAddressRepository` and `UserAddressRepository`
- Left `// NOTE` comments in each file documenting where geocoding now lives

---

### 3. Bug Fix — `RepositoryBase.FindListAsync(predicate, includes[], ...)` Missing Where Clause

**Problem:** The `FindListAsync(Expression<Func<T, bool>> expressionPredicate, Expression<Func<T, object>>[]? includes, bool trackChanges, CancellationToken)` overload applied includes but never called `.Where(expressionPredicate)` — it returned all rows regardless of predicate.

**Fix:** Added `query = query.Where(expressionPredicate)` after `ApplyIncludes`. The bug was caught by the new `RepositoryReadPathTests` suite.

---

### 4. New Test File — `OrganizationSecurityServiceRepositoryTests` (26 tests)

Covers all 7 previously untested methods of `Ben.Service.RepositoryService.Services.OrganizationSecurityService`:

| Method | Tests |
|---|---|
| `SearchUsersAsync` | SuperAdmin sees all · regular user sees only shared org members · query filters on DisplayName · skip/take pagination |
| `HasAccessAsync` | SuperAdmin always true · Owner always true · Administrator always true · member with matching grant · member with no grant · non-member |
| `GetOrganizationsForUserAsync` | SuperAdmin returns all · regular user returns only member orgs |
| `RegisterOrganizationAsync` | Creates org + Owner membership · trims whitespace · throws on blank name · throws when user not found · throws on duplicate urlName |
| `GetOrganizationUsersAsync` | Owner sees members ordered by role · SuperAdmin can access any org · regular member throws `UnauthorizedAccessException` |
| `UpsertMembershipAsync` | New user creates membership · existing user updates role + sets `DateUpdated` · regular member throws unauthorized |
| `SetAccessGrantAsync` | New grant creates entry · second call updates actions + sets `DateUpdated` · non-member target throws `InvalidOperationException` |

Infrastructure: `SeedSuperAdminAsync` helper seeds `IdentityRole<Guid>` + `IdentityUserRole<Guid>` into the in-memory DB to enable SuperAdmin path tests.

---

### 5. New Test File — `RepositoryReadPathTests` (20 tests)

Smoke-tests the full `RepositoryBase<T>` read-path through `UploadFileTypeRepository` (minimal seed requirements, has a collection navigation) and `OrganizationAddressTypeRepository` (cross-entity sanity):

| Group | Tests |
|---|---|
| `GetAllAsync` | Empty · all entities · explicit includes · `includeAllNavigations` |
| `FindListAsync` | Match · no match · predicate + includes · `IsActive` filter |
| `FindOneAsync` | Match · no match · predicate + includes |
| `GetByIdAsync` | Valid ID · unknown ID null · explicit includes · `includeAllNavigations` |
| `CountAllAsync/CountFindAsync` | Empty → 0 · correct count · predicate count |
| Cross-entity | `OrganizationAddressTypeRepository` GetAll + FindOne |

Note: `IncludeAllNavigations` tests seed an `AppUser` to satisfy EF Core's required reference navigation fixup for `CreatedByAppUser`.

---

### 6. Test Standardisation — Factory-Scoped Async Pattern

Three test files were using `TestDbFactory.Create().CreateDbContext()` (synchronous, discards factory immediately). Standardised to match the rest of the suite:

| File | Change |
|---|---|
| `AddressMapConfigTests.cs` | Added `CreateFactory()` helper; `SeedAsync()` now accepts `IDbContextFactory` and uses `await factory.CreateDbContextAsync()`; each test holds `factory` |
| `OrganizationFilePublishAndDeleteLogTests.cs` | Same pattern |
| `OrganizationMembershipRequestTests.cs` | Added `CreateFactory()`; `SeedBasicAsync` return simplified from `(BenDataContext, AppUser, Organization)` → `(AppUser, Organization)` (db already in scope); all 10 tests use `await factory.CreateDbContextAsync()` |

---

## Tests

**219 → 265 tests (+46), all passing, 0 warnings.**

| File | New/Modified | Tests |
|---|---|---|
| `RepositoryManagerTests.cs` | Modified | Removed `SaveChangesAsync_Throws_NotImplemented` |
| `OrganizationSecurityServiceRepositoryTests.cs` | **New** | 26 tests |
| `RepositoryReadPathTests.cs` | **New** | 20 tests |
| `AddressMapConfigTests.cs` | Modified | Factory pattern standardisation |
| `OrganizationFilePublishAndDeleteLogTests.cs` | Modified | Factory pattern standardisation |
| `OrganizationMembershipRequestTests.cs` | Modified | Factory pattern standardisation |

---

## Items Completed (continued — afternoon session)

### 7. Audit Logging — All Custom Write Endpoints

Added `IAuditLogService` and `TryAuditAsync` to all 19 custom write controllers that previously had no audit trail. Added `TryAuditAsync` as a protected static method on `BenControllerBase` so all controllers inherit it once. Added `GlobalUsings.cs` to `Ben.Web.Tests` so `IAuditLogService` is available to test files automatically.

Every Create, Update, and Delete in the API now writes a fire-and-forget `AuditLog` row. Audit failures never surface to callers.

---

### 8. Named Organization Role System (from Organization Security.md — gaps D + E)

**Problem:** The spec described a role-based permission system where org owners/admins create named roles with per-section CRUD flags, assign members to multiple roles, and permissions resolve via OR logic. The codebase only had direct per-user `OrganizationAccessGrant` rows and a fixed `OrganizationMemberRole` enum.

**Implementation:**

**New entities (3):**
- `OrganizationRole` — named role scoped to an organization (Name, Description, IsActive, SortOrder, audit fields)
- `OrganizationRolePermission` — per-table CRUD bitmask (`OrganizationSecurityTable` + `OrganizationSecurityAction`) for a role; unique index per (roleId, tableName)
- `OrganizationRoleMembership` — links an `OrganizationUserMembership` to a named role; unique index per (roleId, membershipId)

**EF migration:** `AddOrganizationNamedRoles`

**Repository layer:** 3 new interfaces + implementations wired into `OrganizationRepositoryManager`

**Security service:** `HasAccessAsync` updated — after checking direct grants, now also queries named role permissions via OR logic (any active role the user belongs to that has matching table+action → true)

**Service models + mappings:** `OrganizationRoleRecord`, `OrganizationRolePermissionRecord`, `OrganizationRoleMembershipRecord` + AutoMapper profiles

**WebApi:** `OrganizationRoleController` at `api/organizations/{orgId}/roles` — 10 endpoints covering role CRUD, replace-all permissions (`PUT .../permissions`), and member assignment

**Blazor (`Ben.Web.Library/Organization/Roles/`):**
- `OrgRolesManager.razor` — grid of roles with create/edit/delete; takes `OrgId` parameter
- `OrgRoleEditor.razor` — role details form + 16-section hierarchical CRUD permission toggles (sub-sections indented with `ms-4`) + member assignment grid
- `OrganizationView.razor` converted to `TelerikTabStrip` — "Details" tab (existing) + "Roles" tab (new); Roles tab visible to SuperAdmin and org Owner/Administrator

**`IBenAdminClient` + `BenAdminClient`:** 10 new org-role API methods

---

### 9. Tests — Named Role Coverage

New test files:
- `OrganizationRoleTests.cs` (14 tests) — entity persistence, cascade deletes, unique index model config for both `OrganizationRolePermission` and `OrganizationRoleMembership`
- `OrganizationRoleControllerTests.cs` (14 tests) — GetAll, Create (valid + blank name), Update (valid + not found), Delete (valid + not found), SetPermissions (replace + None filtering), AddMember, RemoveMember

Added to `OrganizationSecurityServiceRepositoryTests.cs` (4 new tests):
- `HasAccess_MemberWithNamedRoleGrantingPermission_ReturnsTrue`
- `HasAccess_MemberWithNamedRole_WrongTable_ReturnsFalse`
- `HasAccess_MemberWithInactiveRole_ReturnsFalse`
- `HasAccess_MultipleRoles_OrLogic_ReturnsTrueWhenAnyRoleGrants`

---

### 10. SQL Script Regenerated

`scripts/create-database.sql` regenerated via `dotnet ef migrations script --idempotent`. Updated from 2773 → 3209 lines to include all three new `OrganizationRole*` tables.

---

## Tests

**711 → 737 tests (+26), all passing, 0 warnings**

| File | New/Modified | Tests |
|---|---|---|
| `OrganizationRoleTests.cs` | **New** | 14 |
| `OrganizationRoleControllerTests.cs` | **New** | 14 |
| `OrganizationSecurityServiceRepositoryTests.cs` | Modified (+4) | 30 total |

---

## Items Completed (continued — evening session)

### 11. Public Organization Pages at `/o/{urlName}` (Gap I from Organization Security spec)

**Problem:** Spec required org owners to share a link like `https://www.app.com/o/spooky-ben` directing the public to their org home page. No such route existed.

**Chosen approach:** `/o/` prefix (not a catch-all) allows clean sub-page routing without conflicts with existing routes.

**WebApi:**
- `OrgPublicController` — `[AllowAnonymous]` at `api/public/organizations`
  - `GET /{urlName}` — returns org name/urlName, active logos, the `IsHome=true && IsPublished && IsPublic` CMS page with sections, and nav page list
  - `GET /{urlName}/pages/{pageSlug}` — returns a specific public page with sections + nav
- `UploadFileController.Download` — added `[AllowAnonymous]` + serves public files without auth; private files still require authentication

**Client layer:**
- `IWebApiClient.GetAnonymousAsync<T>` — makes requests without auth header
- `IBenAdminClient`: `GetPublicOrgAsync`, `GetPublicOrgPageAsync`, `GetFileDownloadUrl(Guid)` + 6 response records
- `BenAdminClientAdapter`: injects `WebApiOptions` to construct file download URLs; implements all 3 public methods

**Blazor (`Ben.Web.Library/Organization/Public/`):**
- `OrgPublicHome.razor` at `@page "/o/{UrlName}"` — org name, logo, home page CMS sections, nav links to other public pages
- `OrgPublicPage.razor` at `@page "/o/{UrlName}/{PageSlug}"` — org header + specific page with active nav tab highlighted
- `OrgPublicSection.razor` — renders each `CmsSectionType`: `RichText`/`CustomHtml` → `MarkupString` HTML; `ImageBanner` → `<img>` with optional link; others → placeholder stubs

---

### 12. Organization Address Management with Live Geocoding Map Preview

**New address management component:** `OrgAddressManager.razor` added to `OrganizationView` as an "Addresses" tab (same visibility as Roles: Owner/Admin/SuperAdmin).

**WebApi:**
- `GeocodingController` — `GET api/geocode/preview` (requires auth). Accepts address fields as query params, calls `AddressGeocodingService.TryResolveCoordinatesAsync`, returns `{Latitude, Longitude, ResultType}` without touching the database.
- `OrganizationAddressCrudController` — org-scoped `GET/POST/PUT/DELETE api/organizations/{orgId}/addresses`. Owner/Admin/SuperAdmin only. Applies geocoding on Create and Update. Full audit logging.

**Client:** 6 new `IBenAdminClient` methods — `GetOrgAddressesAsync`, `GetOrgAddressTypesAsync`, `CreateOrgAddressAsync`, `UpdateOrgAddressAsync`, `DeleteOrgAddressAsync`, `PreviewGeocodingAsync`.

**`OrgAddressManager.razor` — live map preview behavior:**
- 700ms debounce on any address field change
- Cancels the previous pending geocode on each keystroke
- Shows "Looking up location…" spinner while Nominatim call is in flight
- `AddressMapPlayer` renders below the form with `ReadOnly=true, ShowConfigPanel=false` — a 240px map with a pin that moves to the geocoded coordinates. No coordinates are displayed.
- On edit, restores existing lat/lon from the saved record immediately as the initial preview

---

---

## Items Completed (continued — end of day)

### 13. Geocoding switched from Nominatim to geocod.io

`AddressGeocodingService` rewritten for the geocod.io v2 REST API:
- Forward: `GET /geocode?q=...&api_key=...&limit=1` → `results[0].location.{lat,lng}` + `accuracy_type`
- Reverse: `GET /reverse?q={lat},{lon}&api_key=...` → `address_components.{number,street,suffix,city,state,zip,country}`
- Added `Configure(apiKey, baseUrl)` static initialiser called from `Program.cs` at startup
- Removed Nominatim HTTP client, `NominatimResult` class, and `UrlEncoder` dependency
- API key stored in `appsettings.Development.json` (gitignored) under `Geocodio:ApiKey` / `Geocodio:BaseUrl`

### 14. Lat/Lon precision fix — `decimal(18,2)` → `decimal(18,10)`

**Problem:** EF Core maps `decimal?` to `decimal(18,2)` by default in SQL Server — only 2 decimal places. Latitude `36.094567` was stored as `36.09`, placing the map pin miles off.

**Fix:** Added `HasPrecision(18, 10)` for `Latitude` and `Longitude` on `UserAddress` and `OrganizationAddress` in `BenDataContext.Generated.cs`.

**Migration:** `FixLatLonPrecision` — alters both address tables. Applied to local DB.

**SQL script:** `scripts/create-database.sql` regenerated to include the new migration.

---

## Git (final state)

**Branch:** `feature/organization-enhancements`

| Hash | Summary |
|---|---|
| `1c0075d` | refactor: Option B repository cleanup + geocoding + tests |
| `26ee1e3` | docs: update daily log, project-analysis, README (morning) |
| `9658378` | feat: IAuditLogService audit logging to all custom write endpoints |
| `7d3bb10` | feat: named organization role system |
| `1cd79a0` | tests+docs: OrganizationRole tests, SQL script, project notes |
| `9a25472` | feat: public org pages at /o/{urlName} |
| `f1defd6` | feat: org address management with live geocoding map preview |
| `2c63642` | feat: switch geocoding from Nominatim to geocod.io |
| *(pending)* | fix: lat/lon precision + SQL script + project notes |

**`create-database.sql`**: current — regenerated end of day including `FixLatLonPrecision`.

Previous baseline: `5c562a7`
