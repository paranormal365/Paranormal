# 2026-08-02 — Dev Log

## Summary

Started `feature/public-ui` branch. Goal: build the public-facing discovery and case pages that expose the platform to unauthenticated visitors.

---

## Branch

`feature/public-ui` (off `develop` @ 3d64472)

---

## What Exists

| Area | Status |
|---|---|
| `/o/{urlName}` — `OrgPublicHome.razor` | ✅ Built |
| `/o/{urlName}/{pageSlug}` — `OrgPublicPage.razor` | ✅ Built |
| `GET /api/public/organizations/{urlName}` | ✅ Built |
| `GET /api/public/organizations/{urlName}/pages/{slug}` | ✅ Built |
| `GET /api/public/organizations/{urlName}/cases` | ✅ Built |
| `GET /api/public/organizations/{urlName}/cases/{ref}` | ✅ Built |
| `GET /api/public/organizations/search?lat=&lon=` | ✅ Built |
| `IBenAdminClient` — all three public method groups | ✅ Built |

## What's Missing (This Branch)

| Page | Route | Priority |
|---|---|---|
| `OrgPublicCaseList.razor` | `/o/{UrlName}/cases` | P1 |
| `OrgPublicCaseDetail.razor` | `/o/{UrlName}/cases/{CaseRef}` | P1 |
| `OrgDiscovery.razor` | `/find` | P1 |
| Nav link: "Cases" tab in `OrgPublicHome` | — | P2 |
| Nav link: "Find a Group" on Home page | — | P2 |

---

## Work Log

### Session 3 — Option B: Vote Controller Tests

**Finding:** Both `EvidenceVoteController` and `MembershipReviewVoteController` are already implemented — `EvidenceVoteController` is the second class in `InvestigationController.cs`; membership vote endpoints (`/open-vote`, `/vote`, `/votes`) are in `OrganizationMembershipRequestController.cs`. The gap was tests only.

**2 new test files (24 tests):**
- `EvidenceVoteControllerTests` — 13 tests: GetSummary (counts, current user vote, privacy assertion), GetAll (filter by file), CastVote (new vote, upsert, 404), RemoveVote (204, 404)
- `MembershipReviewVoteControllerTests` — 11 tests: OpenVote (sets IsUnderReview, past deadline, non-pending, 404), CastVote (new, upsert, not under review, deadline passed, 404), GetVotes (empty, with votes, no-permission 403)

**Key learner:** InMemory EF performs effective INNER JOIN for required nav properties in `Include()`. Voter AppUsers must be seeded in the test context for `Include(v => v.VoterAppUser)` to not filter out the vote rows.

**Tests:** 1005 total (354 repo + 651 web) ✅ | **Build:** 0 errors, 0 warnings ✅



**OrganizationView overhaul:**
- Fixed `OrganizationListItemResponse` mismatch: added `IsAcceptingApplications` to client record
- Added `_isMember` flag (org appears in list = user is member; SuperAdmin always member)
- Removed `max-width:640px` from outer container — components now use full width
- Added 6 new tabs: **Members**, **Cases**, **Calendar**, **Messages**, **Files** (all gated on `_isMember`), **CMS** (gated on `_canManageRoles`)
- Details tab now shows `IsAcceptingApplications` badge + public URL link

**`EmbeddedMode` parameter added to 6 components:**
- `OrganizationMembers`, `CaseList`, `OrganizationFiles`, `OrgCmsEditor`, `OrgScheduler`, `OrgMessages`
- When `EmbeddedMode=true`: hides `<PageTitle>`, back-nav buttons, and top section headers
- Standalone routes (`/organizations/{id}/members`, etc.) unchanged — `EmbeddedMode` defaults to `false`

**3 new public controller test files (39 tests):**
- `PublicCaseControllerTests` — 14 tests (invalid ref, unknown org/case, privacy assertions, pseudonym, status filtering)
- `OrgPublicControllerTests` — 15 tests (404 paths, published/public/private page filtering, nav exclusions, case-insensitive slugs)
- `PublicOrganizationSearchControllerTests` — 10 tests (coordinate validation, range/outside-range logic, ordering, maxResults, privacy)

**Tests:** 981 total (354 repo + 627 web) ✅ | **Build:** 0 errors, 0 warnings ✅



**New files:**
- `Ben.Web.Library/Organization/Public/OrgPublicCaseList.razor` — `/o/{UrlName}/cases` — card grid of public cases with status badges
- `Ben.Web.Library/Organization/Public/OrgPublicCaseDetail.razor` — `/o/{UrlName}/cases/{CaseRef}` — case detail with pseudonym support + timeline
- `Ben.Web.Library/Organization/OrgDiscovery.razor` — `/find` — geocoded address search → org cards split by within/outside range

**Modified:**
- `OrgPublicHome.razor` — nav always rendered; "Cases" tab added (no longer gated on `NavPages.Any()`)
- `OrgPublicPage.razor` — "Cases" tab added to sub-page nav
- `MainNavigationDrawer.razor` — "Find a Group" (`SvgIcon.MapMarker`, `/find`) added as permanent nav item

## Telerik 14.1.0 Status

**Version confirmed current** — 14.1.0 (July 15, 2026) is the latest release. No upgrade needed.

**Notable 14.1.0 additions affecting this project:**
- `TelerikExpansionPanel` — new collapsible panel (not in extracted local source; available via NuGet)
- `TelerikMultiSelect` — Tag mode now ON by default, `SelectAll` option added
- `TelerikPopup` — `CloseOnOutsideClick` parameter
- `TelerikRadioGroup` — `DisabledField` per-item disabled state
- `TelerikGrid` — popup edit default button renamed "Update" → "Save"
- `TelerikScheduler` — Delete/Backspace now respect `AllowDelete`; appointment height proportional fix
- `TelerikTabStrip` — `ActiveTabIdChanged` no longer fires on app navigation (was a bug)
- `TelerikWindow`/`TelerikWizard` — keyboard shortcut customization added

---

## Gap Analysis Summary (2026-08-02)

### Data Model
- ❌ No `CaseNote` entity (vision: case managers make notes on cases)
- ❌ `Case.CaseManagerId` FK missing (inferred from permissions only)
- ❌ `Organization` missing public contact fields (PublicPhone, PublicEmail, PublicWebsite)

### API
- ❌ No `EvidenceVoteController` (entity + IBenAdminClient methods exist, controller missing)
- ❌ No `MembershipReviewVoteController` (Phase 3 committee voting wired nowhere)
- ❌ No member case proposal workflow (propose → admin approve)

### UI
- ❌ `OrganizationView` has only 4 tabs — Cases, Members, Files, CMS, Calendar, Messaging, Membership Requests all unwired
- ❌ `Home.razor` nearly empty — no hero CTA or calls to action
- ❌ `AdminSidePanel` missing: Cases, Memberships, Investigations, Client Requests sections

### Tests
- ❌ No `PublicCaseControllerTests`
- ❌ No `PublicOrganizationSearchControllerTests`
- ❌ No `OrgPublicControllerTests`

