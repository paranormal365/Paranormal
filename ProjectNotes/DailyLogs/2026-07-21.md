# 2026-07-21 — Daily Log

## Session Overview

Full-day session. Completed 5 backlog items + a major architectural refactor (file storage migration).

---

## Items Completed

### 1. Wire `AudioFilePreview` into `UserMediaPreview.razor`

- `UserMediaPreview.razor`: added `FileSize` parameter; audio branch now renders `<AudioFilePreview>` (WaveSurfer waveform + full-view modal) instead of plain `<audio controls>`; `_hasPreview` excludes audio so bytes are no longer fetched for audio files
- `AdminUserDetail.razor`: removed `@if (IsAudioContentType(...))` conditional — just `<UserMediaPreview>` with `FileSize` for all file types; removed unused `IsAudioContentType` helper

### 2. `FileExtensionPatternMatcher` client-side validation in `UploadFiles.razor`

- `UploadFileTypeRecord`: added `IReadOnlyList<string> AllowedPatterns { get; init; }`
- `UploadFileTypeProfile` (AutoMapper): maps `AllowedExtensions.Select(e => e.Pattern)` → `AllowedPatterns`
- `UploadFileTypeController`: overrides `GetAll` to `Include(t => t.AllowedExtensions)` so patterns are returned with each type
- `UploadFiles.razor`: `OnFileSelected` validates extension immediately; file type dropdown `ValueChanged` re-validates if type changes after file selection; `UploadFile` re-validates before sending

### 3. `OrganizationAccessGrant` DELETE endpoint

- `IOrganizationSecurityService` (RepositoryService): added `DeleteGrantAsync(orgId, userId, table?, actingUserId, ct)` returning `int` (rows deleted)
- `OrganizationSecurityService`: implementation queries by `orgId + userId`, optionally filters by `tableName`, removes all matching rows
- `OrganizationSecurityController`: `DELETE /api/organizations/{orgId}/security/users/{userId}/grants` + `?table={int}` query param — returns `{ deleted: N }`

### 4. SuperAdmin CRUD for lookup tables (`/admin/lookup-types`)

- `LookupTypeAdminRecord` (Ben.Service.Models): shared record for all 11 lookup-type tables (same schema)
- `LookupTypeUpsertRequest` record added to `IBenAdminClient.cs`
- `IBenAdminClient`: 4 generic methods — `GetLookupTypesAsync`, `CreateLookupTypeAsync`, `UpdateLookupTypeAsync`, `DeleteLookupTypeAsync` (all take `string route`)
- `BenAdminClientAdapter`: implements all 4 via generic `IWebApiClient` methods
- `AdminLookupTypes.razor`: single page at `/admin/lookup-types` — family dropdown (11 entries), sortable grid, Add/Edit/Delete dialogs
- `AdminSidePanel.razor`: "Lookup Types" link added under Content section

### 5. CMS member group + page permission UI

- `IBenAdminClient`: 12 new methods — `GetOrganizationMembersAsync` (→ `OrgMembershipItem[]`), 4 group CRUD, 3 group membership, 4 page permission
- New records: `OrgMembershipItem`, `OrgGroupUpsertRequest`, `PagePermissionCreateRequest`
- `BenAdminClientAdapter`: all implemented; added `using Ben.Data.Common.Enums`
- `OrgCmsEditor.razor`: **Member Groups tab** — grid with Edit/Members/Delete; Create/Edit dialog; Delete confirm dialog; Members dialog (current member list + remove + add from org member dropdown)
- `OrgCmsPageEdit.razor`: **Permissions card** — grid showing View/Edit/Delete flags per grantee; Add Permission dialog (User or Group selector + action checkboxes); Edit Permission dialog; all loaded in parallel with page data

### 6. Filesystem file storage (architectural refactor)

- `IFileStorageService` in `Ben.Data.Common/Interfaces/`: `WriteAsync`, `OpenReadAsync`, `DeleteAsync`, `Exists`, `UserFilePath`
- `LocalFileStorageService` in `Ben.Data.WebApi/Services/`: writes to `{FileStorage:RootPath}/users/{userId}/{storedFileName}`
- `FileMigrationService` in `Ben.Data.WebApi/Services/`: hosted service — on startup, migrates all `UploadFile` rows with `FileData != NULL` and `StoragePath IS NULL` to disk (batches of 20, idempotent)
- `UploadFile` entity: `FileData byte[]?` (nullable), `StoragePath string?` added
- Context: `FileData` marked `IsRequired(false)`; `StoragePath` mapped as `nvarchar(500) NULL`
- `UploadFileRecord`: added `StoragePath`
- `UploadFileController`: Upload writes to disk + sets `StoragePath`; Download reads from disk (fallback to `FileData` for un-migrated rows); Delete removes disk file after DB record deleted
- `UploadFileAudioClipController`: clip preview + clip save use `IFileStorageService`; `AudioClipper.Clip` updated to accept `Stream` (no more in-memory `byte[]` required)
- `appsettings.Development.json`: `"FileStorage": { "RootPath": "/Users/ben/Source/Ben/.uploads" }`
- `Program.cs`: `AddSingleton<IFileStorageService, LocalFileStorageService>()` + `AddHostedService<FileMigrationService>()`
- **Migration `20260721133025_AddFileStoragePath`** applied to DB

---

## Tests

| Test File | New Tests | Total |
|---|---|---|
| `DeleteGrantTests.cs` (new) | 4 | 4 |
| `LocalFileStorageServiceTests.cs` (new) | 10 | 10 |
| **Total session** | **+13** | **608** |

Previous: 595 (188 repo + 407 web) → **608 (192 repo + 416 web)**

---

## Database

- Migration `20260721133025_AddFileStoragePath` applied
- `UploadFiles.StoragePath nvarchar(500) NULL` added
- `UploadFiles.FileData` remains (nullable); will be dropped after `FileMigrationService` confirms all rows migrated

## Next Steps

1. Start the WebApi to trigger `FileMigrationService` (migrates existing file blobs from DB to disk)
2. Confirm all `UploadFile` rows have `StoragePath` set
3. Run `dotnet ef migrations add DropFileData ...` to drop the `FileData` column
4. CMS member group + page permission UI end-to-end testing
5. `OrganizationAccessGrant` UI in `OrganizationSecurity.razor`
