# Store sellers (backlog 251), branch `store-sellers`

Taken from `storefront` on 09/24/2026. It won't merge until the store ships. **The plan** is at
`~/.claude/plans/lively-jingling-peach.md` (approved 09/24/2026): sixteen phases, P0–P15. Ben's decisions
and the defaults for the smaller questions are recorded there, and summarised below.

## Goal
Member sellers (the additive Seller site role) list their own items in the store and ship their own
packages. The site takes payment, files the tax, and pays each seller by hand against a tracked earnings
ledger. Alongside that, every product gains:
- a parts list and cost basis;
- seller files that buyers can download;
- FAQs and questions to the seller;
- versions;
- a reviews switch;
- return and warranty text;
- videos.

## Ben's decisions (09/24/2026)
- **Payouts:** by hand. The site is the merchant of record, and an admin marks a seller paid. There's no Stripe Connect.
- **Packages:** each seller ships their own package. The buyer pays the flat rate per package, and a package ships free once its items reach the free-shipping amount.
- **Status:** an order is **Partially shipped** while some of its packages have shipped and others haven't. Each package has its own carrier and tracking, or "No tracking provided". Checkout tells the buyer when an order will arrive in more than one package.
- **Labels:** the seller buys the label and is credited the package's shipping, or the flat rate if the package shipped free.
- **Tax:** always worked out from the site's own address.
- **What the buyer sees:** "Package n · Ships from {seller's display name}".
- **What a seller earns:** their cost basis plus the asking price they propose in the sale request, fixed at checkout. The site sets the selling price and absorbs discounts and Stripe fees.
- **What a seller can do:**
  - Add drafts, and delete a draft that has never sold.
  - Manage options and variants (a new variant starts off, unpriced) and their own stock.
  - See the price but never change it.
  - Take an item off sale.
  - An admin approves and prices the item before it goes on sale.
- **Seller files:** each file is marked for buyers or kept private.
- **Point 8** of Ben's list becomes the seller's earnings summary.

## Phases (status)
| Phase | What | Status |
|---|---|---|
| P0 | Groundwork: sale counters (bug: product `UnitsSold` was never written) and the shared package-pricing function | Built 09/25/2026 — StoreStockTests + `A_sale_counts_on_the_product_too_and_says_when` (1 break caught), migration StoreProductSaleCounters (backfill), StoreParcelPlan + StoreParcelPlanTests 7 (3 breaks caught); StoreLowStockJobTests made date-independent (it failed from midnight UTC 09/25); unit suite green (7,457) |
| P1 | Seller role end to end, read-only workspace, help by role, demo seller | Built 09/25/2026 — Seller policy (a SuperAdmin is NOT a seller), `IsSeller` through Me → token → user state → persisted layout state, `api/seller/store/products` behind `SellerStoreControllerBase.Mine`, Selling → My Items at `/store/selling`, help `role:` front matter, Hazel Marsh seeded with one item on sale and one draft; unit tests (SellerHandler, seller controller, guards, help by role, seeder) with 5 breaks caught; Playwright StoreSellerWorkspaceTests 2/2, both shown failing against a leaking list and an unguarded page; unit suite green (7,479) |
| P2 | Product history | Built 09/25/2026 — append-only `StoreProductChange` (migration StoreProductHistory, backfills a "Created it." line per product), `StoreProductHistory` writes one plain sentence per area in the same save as the edit (stock wrapped in one transaction with its line); every admin product write and both stock-page writes leave a line, an empty or refused save leaves none; admin History tab (shared `Store/Editor/StoreProductHistoryList`) and `api/seller/store/products/{id}/history` (no price lines, staff shown as "The store"); the seller's tab arrives with the seller editor in P3. StoreProductHistoryTests 11 + EveryProductWriteLeavesHistoryTests 3, 7 breaks caught; Playwright `The_history_tab_says_who_changed_what` (shown failing when the tab doesn't load); AdminStoreCatalogTests + StoreSellerWorkspaceTests 12/12; unit suite green (7,493); help: site-administration "A product's history"; go-live and deploy docs name the migrations |
| P3 | Seller editing, sale requests, taking an item off sale (also extracts the shared editor pieces) | Built 09/25/2026 — every product rule moved from the admin controller into `StoreProductEditor` (+ `StoreProductSale`, `StoreProductRecords`), shared by `SellerStoreProductEditController`; seller requests carry no store field, a seller's new/generated variant starts off at $0.00 and their saves keep the store's price; `StoreProductSaleRequest` (one open per item, filtered unique index + conditional-update claims) with `/admin/store/sale-requests` (readiness list, approve fixes `SellerAskPerUnit`, decline needs a reason), nav entry, dashboard tile, Waiting badge; admin Put on sale refused on a seller's item; stale-edit 409 on details/options saves (both editors); seller editor `/store/selling/items/{id}` and Add an item, built from shared `Store/Editor` components (specs, options, variants grid, pictures, stock dialog, preview) that the admin editor now uses too (829 → ~590 lines); `StoreSellerAlerts` bells both ways; migration StoreSellerSaleRequests (FirstOnSaleUtc backfill). StoreSaleRequestTests 19, guards extended (history through the editor, refusals through shared components), 10/11 breaks caught (the survivor is equivalent: approval re-checks inside the switch and rolls the claim back); Playwright Store category 92 + AdminPageTests green incl. `A_draft_goes_on_sale_by_asking_and_comes_off_by_the_seller`; StoreBrowseTests' sold-out test now finds its card by name — P0's real sold counts reorder "Popular"; unit suite green (7,519) |
| P4 | Parts list and cost basis | Built 09/25/2026 — `StoreProductPart` (per-pack or per-piece price to 4 places, pieces a pack, used a unit, links, picture, on hand) + product `OtherCostPerUnit`/note (migration StoreProductParts); shared pure `StoreCostMath` (rounds once) used by the server and the editors' live total; `StoreCostBasisReader`; Parts & cost tab in both editors (`Store/Editor/StorePartsEditor`), history area Parts; Hazel's REM pod seeded at $34.50 a unit (each seeded piece now idempotent). StoreCostMathTests 4, StorePartsTests 9, StorePublicRecordsCarryNoCostTests; 6/6 breaks caught; Playwright `The_parts_tab_says_what_a_unit_costs_to_make`; unit suite 7,533 |
| P5 | Packages on the server | Built 09/25/2026 — `StoreOrderParcel` (number, seller + name snapshot, status Waiting/Packed/Shipped/Delivered/Cancelled, shipping, its share of shipping tax, shipping refunded, seller's label credit, carrier/tracking/times), `ParcelId` on order items and events (NoAction); migration StoreOrderParcels backfills one package per existing order from the order's own columns; checkout prices packages with `StoreParcelPlan` (same plan as the cart), writes one package per seller in the placing transaction, credits a seller the flat rate even when their package shipped free, splits the shipping tax exactly; the fingerprint now carries each line's seller; reuse rewrites each package's shipping; tax filed on an earlier calculation re-splits the packages' tax; `StoreCartView.Parcels` ("Package n · Ships from …", MoreForFree); seeded orders get their package. StoreParcelCheckoutTests 7 (incl. `The_cart_and_checkout_price_packages_the_same`, rollback, reuse, tax re-split); 5/5 breaks caught (one after strengthening a cart check); unit suite 7,540 |
| P6 | Packages for the buyer | Built 09/25/2026 — shared `Store/Shared/StoreParcelRows` draws a shipping row per package (with "Add $X more from {seller} to ship this package free" on cart surfaces) under the Shipping row of the header menu, drawer, order summary (cart + checkout), thank-you page and order page, and on the invoice; a single package draws nothing new; checkout says "Your order will arrive in N packages" with each package; `StoreCheckoutTotals.Parcels` and `StoreOrderView/StoreInvoiceRecord.Parcels` (`StoreOrderParcelView`); the single-package free-shipping note now reads the package's own MoreForFree. Guard `Every_surface_draws_a_row_per_package`; Playwright `Two_packages_through_checkout_to_the_order_page` (shown failing with the rows switched off) and `One_package_has_no_package_rows`; Store category 91/0 (2 real-Stripe skipped) |
| P7 | Shipping each package, and Partially shipped | Built 09/25/2026 — `StoreOrderStatus.PartiallyShipped = 7` (labels, tone, filters via the enum, To-ship tile, needs-action filter); pure `StoreParcelRollup`; `StoreParcelTransitions` packs/ships/corrects/delivers ONE package in a transaction that writes the order row first (lock), moves the package, then rolls the order up — the order-level moves are gone; admin routes `…/orders/{id}/parcels/{parcelId}/pack|ship|tracking|deliver`; a card per package on the admin order page (single package keeps `#order-ship` etc.) and on the buyer's order page; one shipped letter + bell per package ("package 1 of 2", only its items; mail field Package); cancel refused once anything shipped, address locked, address scrubbed when every package is delivered, unsent packages cancelled with their order; new mail kind + starter `store-seller-parcel-to-ship` and a bell to each seller when paid; `SellerStoreParcelController` (`api/seller/store/parcels`, own packages only, address until delivered) and `/store/selling/packages` (My Packages); migration StoreOrderTrackingToParcels copies any order tracking to package 1 and drops the order columns; desk search/CSV read packages; seeded two-package (Partially shipped) and delivered Hazel orders. StoreParcelRollupTests 9, StoreParcelTransitionsTests 9 (incl. concurrent ship and a source check of the lock order — SQLite serialises writers, so the race only shows on SQL Server); transitions/controller/seeder tests moved onto packages; 7/7 breaks caught; Playwright `Each_package_ships_on_its_own` (admin ships 1 → Partially shipped; Hazel ships 2 from My Packages → Shipped; shown failing with the roll-up broken); Store category + AdminPageTests green after stabilising `Listing_FiltersAndPagesThroughTheQueryString` (its sort step lost the change to a swapped-in page once the e2e DB held ~465 products); unit suite 7,565 |
| P8 | Refunds with packages | Built 09/25/2026 — `StoreRefundShipping` rows (migration StoreRefundShipping) and `StoreRefundRequest.ShippingForParcels`: a package's shipping and its tax go back once, the last refund taking what is left (`StoreRefundService.ShippingRemaining`), counted onto the package's `ShippingRefunded` only when Stripe completes; `StoreOrderTransitions.CancelParcelAsync` + `POST …/parcels/{id}/cancel` refund one unsent package's items and shipping (restock optional) and, on completion, mark it Cancelled and roll the order up (the only live package can't be cancelled alone); whole-order cancel gives back every package's shipping; admin refund dialog has per-package shipping boxes and each package card a "Cancel package…" button. StoreParcelRefundTests 5, 5/5 breaks caught (one after adding the partly-shipped case); StoreAdminOrderTests 7/7; unit suite 7,570; help "Refunds and packages" |
| P9 | Economics and the Stripe fee | Built 09/25/2026 — each order line fixes `UnitCostBasis`, `UnitSellerAsk`, `UnitSellerEarning` (cost + ask) and `UnitSiteMarkup` at checkout; pure `StoreEconomics` (fee estimate, earning, markup, suggested price covering markup and fee, below-cost check); store settings "Prices and fees" (markup %, card fee %, per-payment fee; defaults 30 / 2.9 / $0.30) through reader, validation, labels and the settings page; `IStoreStripeGateway.GetChargeFeeAsync` (real: the intent's latest charge's balance transaction; fake: 2.9% + 30¢), `StoreOrderPayments.CaptureFeeAsync` after payment, `StoreFeeCaptureJob` sweeping for a fortnight; order `StripeFeeAmount`/`StripeNetAmount` (migration StoreEconomics, backfilling old lines' markup to their price); admin economics panel on a product's Parts & cost tab (`GET …/economics`) and an Economics card on the order page; seeded Hazel orders carry their economics; the flat-rate setting text now says per package. StoreEconomicsTests 9, 6/6 breaks caught; Playwright `The_economics_panel_says_what_each_side_gets` + StoreAdminOrderTests + StoreCheckoutTests 18/18; unit suite 7,579; help "Economics and fees" |
| P10 | Earnings ledger, payouts, seller summary | Built 09/25/2026 — append-only `StoreSellerEarning` (Sale, Shipping, Refund, Adjustment; filtered unique indexes so a sale, a label and a refund line are each written once) and `StoreSellerPayout` (migration StoreSellerEarnings); `StoreSellerLedger`: earnings written when a seller's package ships (in the shipping transaction; units already refunded aren't earned), a refund after shipping writes a negative line, payments claim unpaid lines up to a cutoff with one conditional update and total them in C#, refused when the figure changed since the page opened, when nothing is owed or when the seller owes the store (carried forward); void releases the lines; adjustments with a note; default cutoff = today − returns window; `/admin/store/sellers` (balances) and `/admin/store/sellers/{id}` (Record payment…, Adjustment…, Void…, CSV) behind `AdminStoreSellerController` (admin controller count 10); the seller's `/store/selling/earnings` (Ben's point 8: owed, ready, waiting, paid; by item, order, payment, year) via `SellerStoreEarningsController`, both drawing the shared `SellerEarningsView`; nav entries; account closure anonymises the seller name on packages and tells the SuperAdmins about unpaid earnings; seeded Hazel earnings (backfilled for packages that went before earnings were kept). StoreSellerEarningsTests 6, 8/8 breaks caught; Playwright `Record_a_seller_payment` + seller/admin/catalogue tests 18/18; unit suite 7,591; help "Paying sellers" and "Your earnings" |
| P11 | Seller files and buyer downloads | Built 09/25/2026 — `StoreProductFile` (migration StoreProductFiles): kind (Manual, Firmware, Software, Document, Other), audience **Buyers** or **Private** (Ben's per-file choice), title, version, and either an upload (new "Store Product File" type `A0000000-…-0002`, ownerless, never public, stored at `store/products/{id}/files/`, up to 95 MB) or a manual written on the site (Markdown via Markdig, or imported from .md/.txt, then sanitised). Editor methods add/write/change/remove with history lines ("Gave buyers …", "Made … private."); a removed file's bytes go unless something else still holds them. Routes on both editors' controllers; `StoreProductFileController` (`api/store/product-files/{id}/download|manual`) for the store's staff and the item's seller only (404 to anyone else; not behind the switch, reason recorded in the gating guard); buyer downloads on `StoreOrderController` (`{id}/downloads`, `/downloads/{fileId}`, `/downloads/{fileId}/manual`) via `StoreOrderDownloads`: paid, not cancelled, line not wholly refunded, audience Buyers. Website proxies `/media/store-files/{id}` and `/media/store-downloads/{order}/{file}` (ticket and/or the order's link token), `IMediaUrlBuilder.StoreProductFile/StoreOrderDownload`. Shared `StoreFilesPanel` (Files tab in both editors, `?tab=files`), Downloads on the buyer's order page, printable `StoreManual` page (`/store/orders/{id}/manuals/{file}` and `/store/manuals/{file}`). Seeded: Hazel's "REM Pod quick start" (buyers) and "Build notes" (private). StoreProductFileTests 10, 16/16 breaks caught (after one strengthened assertion); Playwright StoreProductFilesTests 3/3 — the seller test first caught the page not re-rendering after the panel's Func callbacks; help "Files", "Product files", "Downloads" |
| P12 | FAQs and questions | Built 09/25/2026 — `StoreProductFaq`, `StoreProductQuestion` and `StoreProduct.FaqEnabled` (migration StoreProductQuestions, switch default on). FAQ saved whole from a shared `StoreFaqEditor` (FAQ tab in both editors, `?tab=faq`), one history line per save (area Faq: "FAQ: added 1, changed 1, removed 1.", "Switched the FAQ off."). `StoreProductQuestions`: signed-in shoppers ask about a live item (not their own; 3 open per item, 10 a day), `MyStoreQuestionController` (`api/me/store/questions`, gated, cart rate limit); a seller's item goes to its seller (`SellerStoreQuestionController`, `/store/selling/questions`), the store's own stock to every SuperAdmin (`AdminStoreQuestionController`, `/admin/store/questions`, "The store's own"/"Every item"; admin controller count 11); answering/declining is a conditional claim (answer once), the asker's bell and "My questions" page (`/store/questions`); "Add to the FAQ…" copies a reworded answer once, recorded in history. Anonymity by shape — `StoreReceivedQuestionRecord` has no asker, bells come from the store. The product page shows "Questions and answers" after the reviews with Ask a question (sign-in offered once auth is known). Closing or purging an account removes its questions (census updated); a promoted FAQ stays. Nav: My Questions, Selling → Questions, Store → Questions. Seeded: Hazel's two FAQ entries, an answered+promoted question, one waiting for her, one about site stock. StoreProductQuestionTests 9, 18/18 breaks caught; Playwright StoreQuestionsTests 2/2 (caught the ask button reading sign-in before AuthReady) and the seller workspace test now waits for the variant save it depends on; help "FAQ and questions" ×2, "Asking a question" |
| P13 | Versions | Built 09/25/2026 — `StoreProduct.PreviousVersionProductId` (unique filtered self-reference, NoAction), `VersionLabel`, `SupersededPolicy` (KeepOffering / SellOut / Discontinue — Ben's three), `SupersededAppliedUtc`, `SellingOutSinceUtc`, `DiscontinuedUtc` (migration StoreProductVersions). `StoreProductCopier` (Duplicate now uses it) copies words, options, variants (new SKUs with the label's suffix, no stock), specs and pictures — and for a new version the parts, FAQ and files (same bytes) and the same seller. `StartVersionAsync` (only for an item that's been on sale; one newer version each), `SaveVersionAsync` (name any time; policy until applied). The policy fires once, in `SwitchAsync` on the new version's first day on sale (so approval triggers it too): discontinue takes the old off at once; sell-out leaves it until nothing is on hand (immediately if already empty) and `StoreSupersededSweepJob` finishes it; keep-offering leaves both. Putting an old version back on sale by hand clears its discontinued state; a product a newer version links to can't be deleted. Public page: a discontinued version keeps its page ("No longer made — replaced by", never buyable, no Ask), "A newer version is available" banner, version badge, "Previous version" link; the parent-category guard caught two of the new queries. Shared `StoreVersionPanel` (Versions tab in both editors, `?tab=versions`, Start a new version… dialog → the new draft's editor). History area Versions. Seeded: Field Thermometer v1 (discontinued) → v2 (on sale); seeder/public-controller counts updated. StoreProductVersionTests 7, 18/18 breaks caught; Playwright StoreVersionsTests 2/2 and the whole Store category 101/101 (2 real-Stripe skipped); help "Versions" ×2, "Newer versions" |
| P14 | Page extras | Built 09/25/2026 — `StoreProduct.ReviewsEnabled` (store-only; migration default on), `ReturnPolicyText`, `WarrantyText` (plain text, 2,000 each) and `StoreProductVideo` (mp4/webm/mov, 95 MB, three a product) — migration StoreProductPageExtras. Reviews off: `StoreRatingCaches` counts only switched-on items, so stars leave cards, sorts and the rating filter too; the detail and the review list show none; writing one answers 409 "This item doesn't take reviews."; switching on recomputes. The seller's save has no reviews field at all (`SaveSellerExtrasRequest`). Videos are ingested with A/V stripping (`MediaIngestService`), the stripped copy served by the new ungated `PublicStoreVideoController` (`api/public/store-video/{id}`, ranges, cached a year, only files a product's video holds — reason recorded in the gating guard) through `/media/store-video/{id}`; the gallery shows them as slides after the pictures. A new version copies the words, the switch and the videos. Shared `StorePageExtrasPanel` (Page tab in both editors, `?tab=page`), history area Page. Seeded Hazel's returns and warranty words. StoreProductExtrasTests 4, 16/16 breaks caught (a working stripper stub proves the served copy is the stripped one); Playwright StorePageExtrasTests 3/3 (words, a video uploaded/played in ranges/removed, the switch) with product and review neighbours 13/13; help "The item's page" ×2 and a line under Reviews |
| P15 | Help, screenshots, final runs | |

## Rules that bit
- (P0) Pulling the editor pieces out into shared components moved to P3, where the seller editor first uses them. Pulling them out earlier, with nothing else using them yet, would only move code around without being able to prove the move worked.
