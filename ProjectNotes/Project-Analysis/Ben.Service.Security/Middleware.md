# Ben.Service.Security — Middleware

The `Ben.Service.Security/Middleware/` folder is currently **empty**.

Organisation-level security enforcement is handled via the [`OrganizationSecurityAuthorizeAttribute`](Attributes.md) MVC filter rather than ASP.NET Core middleware.  
No middleware components exist in this project at this time.

---
