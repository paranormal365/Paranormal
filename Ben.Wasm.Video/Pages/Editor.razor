@page "/"
@page "/editor"

<PageTitle>Video Editor</PageTitle>

@* ShowDiagnostics is left unset, so it falls back to the registered option, which is off. This
   host has no role information at hand — sign-in here yields tokens, not claims the page reads —
   so the operator tools stay hidden rather than being shown to whoever happens to be signed in.
   Wiring them to an administrator check is item 96. *@
<VideoEditor />
