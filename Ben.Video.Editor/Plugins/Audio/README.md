# Audio Plugins

Add audio clip effect plugins here as classes implementing `IClipEffect`.

Register each plugin in `ServiceCollectionExtensions.AddBenVideoEditor()` via `ClipEffectRegistry.Register(...)`.
